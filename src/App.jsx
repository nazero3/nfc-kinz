import { NfcCard } from './components/NfcCard'

/**
 * Local demo shell. In kinz-ed, mount <NfcCard /> on a route (e.g. /card)
 * instead of using this App file.
 */
export default function App() {
  return <NfcCard />
}
